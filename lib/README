
This directory contains local project libraries.

For this project (ESP-IDF only), prefer one of these options:

1. Keep reusable code as ESP-IDF components under `components/`.
2. Keep board-/project-specific code under `src/` and `include/`.

If you add custom libraries here, ensure they are integrated through the
ESP-IDF CMake build (directly or via a component CMake file).
